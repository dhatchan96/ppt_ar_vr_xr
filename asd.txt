@dag.cellfn
def TemplateParams(self):
    build = self.Build()
    regressionJira = build.externalSystemIds.get("regressionJiraTicket", "")
    jiraTable = self.JiraTicketsTable()
    jira_tickets = {} if jiraTable is dag.NO_VALUE else jiraTable
    jira_changes = self.JiraChanges()
    popup_message = self.Popup()

    # Fetch linked JIRAs for each ticket
    linked_jiras = {}
    if jira_tickets:
        for ticket in jira_tickets:
            # Assuming `fetch_linked_jiras` fetches the linked JIRAs for a given ticket ID
            linked_jiras[ticket["Ticket"]] = self.fetch_linked_jiras(ticket["Ticket"])

    return dict(
        build=build,
        jira_tickets=jira_tickets,
        jira_changes=jira_changes,
        regressionJira=regressionJira,
        popup_message=popup_message,
        linked_jiras=linked_jiras,  # Pass linked JIRAs to the template
    )
