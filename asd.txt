{% set tooltip_text = "This button is disabled" if is_disabled else "Click to proceed" %}

<button
    title="{{ tooltip_text }}"
    class="btn btn-primary"
    {{ 'disabled' if is_disabled else '' }}
>
    Submit
</button>
