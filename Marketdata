function checkAndSubmitForm() {
  // Get all dropdowns
  const dropdowns = document.querySelectorAll("select");

  // Debug: Log the current values of all dropdowns
  console.log("Dropdown values:", Array.from(dropdowns).map(dropdown => dropdown.value));

  // Check if all dropdowns have been selected (none is empty)
  const allFilled = Array.from(dropdowns).every(dropdown => dropdown.value !== "");

  console.log("All dropdowns filled:", allFilled);

  // If all dropdowns are filled, submit the form
  if (allFilled) {
    console.log("Submitting form...");
    document.getElementById("dropdownForm").submit(); // Submit the form
  }
}

// Attach the `onchange` event listener to all dropdowns when the page loads
document.addEventListener("DOMContentLoaded", () => {
  const dropdowns = document.querySelectorAll("select");
  dropdowns.forEach(dropdown => {
    dropdown.addEventListener("change", checkAndSubmitForm); // Attach event listener
  });
});
