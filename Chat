const handleChange = (sectionName, data) => {
  const updated = { ...formData, [sectionName]: data };
  setFormData(updated);
  localStorage.setItem("aesFormData", JSON.stringify(updated)); // 👈 add this
};
