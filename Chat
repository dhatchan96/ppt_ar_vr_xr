def createApplicationModule(self, name, parentID=None):
    data = {"name": name}
    if parentID:
        data["parent"] = {"type": "application_module", "id": parentID}

    return self.endpoint.post("application_modules", data={"data": [data]})
